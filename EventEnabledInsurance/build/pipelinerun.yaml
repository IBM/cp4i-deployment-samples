apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  name: eei-build-pipelinerun-{{UID}}
spec:
  pipelineRef:
    name: eei-build-pipeline
  workspaces:
    - name: git-source
      persistentvolumeclaim:
        claimName: git-workspace-eei
